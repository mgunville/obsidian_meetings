# Local Models

Local model artifacts for transcription backends live under this folder.

- `whisperx/faster-whisper-base` is expected to be a symlink or local folder.
- Use `scripts/bootstrap_whisperx_model.sh` to link/download a model.
- Model binaries are intentionally ignored by git.
